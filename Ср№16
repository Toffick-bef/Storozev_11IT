№1
word = input("Введите строку из слов: ")

words = set(word.split())

print(f"Количество:{len(words)}")
alphabet = sorted(list(words))
print(f"слова:{alphabet}")

№2
id_allowed = "10 20 30"
id_incoming = "20 40 10 50"
set_allowed = set(id_allowed.split())

for id in id_incoming.split():
    if id in set_allowed:
        print("OK")
    else:
        print("ADDED")
